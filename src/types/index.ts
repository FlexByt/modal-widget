export enum ReadOnlyRatingSize {
  SMALL,
  BIG,
  MEDIUM,
}

export enum Placeholder {
  BOTTOM = "div[data-hook='bottom-placeholder']",
  DETAILS = "div[data-hook='details-placeholder']",
  EMBEDDED = "reviewsJet-embedded",
  PRODUCT_ITEM_DETAILS = "div[data-hook='product-item-product-details']",
}

export enum ReviewsJetPlatform {
  WIX = "wix",
  SHOPIFY = "shopify",
  WEBFLOW = "webflow",
  OTHERS = "others",
}

export enum ReviewsJetTab {
  BUSINESS,
  PRODUCT,
  FAQs
}
